---
layout: null
permalink: /feed.xml
---

{% assign posts_coll = site.posts | default: site.collections.posts.docs %}
{% if posts_coll and posts_coll.size > 0 %}
  {% assign posts_sorted = posts_coll | sort: "date" | reverse %}
{% else %}
  {% assign posts_sorted = empty %}
{% endif %}

<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title | xml_escape }}</title>
  <link href="{{ site.url }}{{ site.baseurl }}/" rel="alternate"/>
  <link href="{{ site.url }}{{ site.baseurl }}/feed.xml" rel="self"/>
  <updated>
    {% if posts_sorted.size > 0 %}
      {{ posts_sorted[0].date | date_to_xmlschema }}
    {% else %}
      {{ "now" | date_to_xmlschema }}
    {% endif %}
  </updated>
  {% for post in posts_sorted limit:20 %}
    <entry>
      <title>{{ post.title | xml_escape }}</title>
      <link href="{{ site.url }}{{ site.baseurl }}{{ post.url }}"/>
      <updated>{{ post.date | date_to_xmlschema }}</updated>
      <id>{{ site.url }}{{ site.baseurl }}{{ post.id }}</id>
      <content type="html">{{ post.content | xml_escape }}</content>
    </entry>
  {% endfor %}
</feed>