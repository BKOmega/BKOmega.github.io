source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8" # for local `jekyll serve` on Ruby 3+

# Optional: local preview of just-the-docs (remote_theme will still work without this)
gem "just-the-docs"
